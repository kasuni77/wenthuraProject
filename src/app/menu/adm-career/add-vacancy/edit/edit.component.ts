import {AfterViewInit, Component, Inject, Input, OnInit, Output, ViewChild} from '@angular/core';
import {NB_WINDOW_CONTEXT, NbWindowRef} from '@nebular/theme';
import {Vacancy} from '../../../../model/vacancy';
import {VacancyService} from '../../../../services/vacancy.service';




@Component({
  template: `

  `,
  styleUrls: ['edit.component.scss'],
})
export class EditComponent  {

  // public id : number;
  // vacancy: Vacancy = new Vacancy();
  //
  //
  // constructor(@Inject(NB_WINDOW_CONTEXT) context, public windowRef: NbWindowRef, private vacancyService: VacancyService) {
  //    this.id = context.id;
  // }
  //
  // ngOnInit(): void{
  //   console.log(this.id);
  // }
  //



  // tslint:disable-next-line:typedef

}

